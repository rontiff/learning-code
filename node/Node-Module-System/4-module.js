
// In Node, every files is a module. 

// The variable and function defined in that file are a "SCOPE" to that module. Not available outside of that module. 

// Every Node application has at least one file or one module which we call "Main Module"

console.log(module)